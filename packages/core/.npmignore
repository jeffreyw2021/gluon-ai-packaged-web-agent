# Prisma generated client — regenerated by postinstall on the user's machine
# for their specific platform. No need to ship these (saves ~95 MB).
prisma/generated/

# Dev sources and config not needed at runtime
src/
tsup.config.ts
tsconfig.json
eslint.config.mjs
node_modules/
